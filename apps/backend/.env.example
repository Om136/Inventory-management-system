# Backend environment (apps/backend)

# Required: Neon / Postgres connection string
# Example: postgres://USER:PASSWORD@HOST:5432/DB?sslmode=require
DATABASE_URL=

# Optional: API port (default 4000)
PORT=4000

# Optional: CORS configuration
# - In production, CORS is DENIED by default unless you set CORS_ORIGIN.
# - Use a comma-separated list of allowed origins.
#   Example: https://my-frontend.vercel.app,https://mydomain.com
CORS_ORIGIN=

# Optional: set to 1 when behind a proxy (Render/Railway/NGINX). Enabled automatically in production.
TRUST_PROXY=1
